// interface variable are by default public,static,and final.
// interface object has also not been created we have to implement in another class then we can override their method.
// interface method has no body.

public interface B {
    void show();

}

public class A implements B {
    void show() {

    }

    public static void main(String[] args) {
    }
}